#include <iostream>
using namespace std;
class percentage
{
private:

    int marks, total;


public:

    void getmarks()
    {
        return marks;
    }
    void gettotal()
    {
        return total;
    }
    void set(int m, int t)
    {
        marks = m;
        total = t
    }
    void perce(m, t)
    {

        double p = m * 100 / t;
        return p;
    }

}
int main()
{
    int marks, total;
    double p;
    getmarks();
    gettotal();
    percentage(marks, total);
    set(marks, total);
    perce(marks, total)
}